// Presenting all of the products that we can order

import React from 'react';
import { FlatList } from 'react-native';

const ProductsOverviewScreen = (props) => {
    return <FlatList />;
};

export default ProductsOverviewScreen;